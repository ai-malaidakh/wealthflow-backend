spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/family_finance_dev
    username: postgres
    password: postgres
  jpa:
    show-sql: true

app:
  jwt:
    secret: dev-secret-key-must-be-at-least-256-bits-long-for-hmac-sha256

sentry:
  dsn: ""
  enabled: false
